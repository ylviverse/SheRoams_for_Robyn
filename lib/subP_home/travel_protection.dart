import 'package:flutter/material.dart';

class TravelProtection extends StatelessWidget {
  const TravelProtection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}